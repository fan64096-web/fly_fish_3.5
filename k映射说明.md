# 分区子域 PDE 残差 · k 映射说明

> 阶段 2（第 3 周）交付物之一 · 2026-08-08
> 目标：实现 3.5D 异构材料热传导约束，让不同区域/不同层用不同热导率 k 算 PDE 残差。

---

## 一、改动总览

| 文件 | 改动 |
|---|---|
| `k_map.py` | **新增**，mask → k 映射模块 + 3D k 场构造 |
| `heat_surface.py` | `apply_model_deepoheat_st` 新增 `k_field` 参数；3d5 模式 pde_res 用 `(k·∇²T)²` |
| `heat_volumetric.py` | 同样接入 `k_field`，3d5 模式各 z 段乘分区 k |
| `models.py` | **未改动**（阶段 1 已完成） |

---

## 二、k 映射表（用户给定，保留原始值）

| 区域 ID | 位置（2×2 布局） | 原始热导率 k [W/(m·K)] |
|---|---|---|
| 0 | 左下 | 130 |
| 1 | 右下 | 130 |
| 2 | 左上 | 150 |
| 3 | 右上 | 1.5 |

> 区域 0/1 同值（两片相同芯片），区域 2 略高（另一芯片），区域 3 很低（封装/中介层，导热差）。

### 缩放说明（数值稳定）

原始 k（130/150/1.5）与原版 surface 的均匀 k=0.1 差 **1000 多倍**，直接用于 PDE 残差会让数值爆炸、训练不稳。因此参与训练的 k 统一缩小：

```
实际 k = 原始 k ÷ K_SCALE   （K_SCALE = 100）
```

- 缩放**不改变各区域 k 的相对比例**（130:130:150:1.5 的比例不变）。
- 物理含义（谁导热好、谁差）完全保留。
- K_SCALE 可在 `k_map.py` 顶部修改。

### 各区域参与训练的 k（÷100 后）

| 区域 ID | 原始 k | 实际 k |
|---|---|---|
| 0 | 130 | 1.30 |
| 1 | 130 | 1.30 |
| 2 | 150 | 1.50 |
| 3 | 1.5 | 0.015 |

---

## 三、z 方向分层（不同层不同 k）

用户要求不同 z 层用不同 k。实现上：区域基础 k 再乘一个 **z 分层系数**。

surface 高度 z ∈ [0, 0.5]，分成 3 段：

| z 范围 | 层 | 系数 |
|---|---|---|
| z < 0.17 | 底层 | ×1.0 |
| 0.17 ≤ z < 0.33 | 中层 | ×0.7 |
| z ≥ 0.33 | 顶层 | ×0.5 |

示例（区域 3，k=1.5 → ÷100 → 0.015）：

| 层 | 实际 k |
|---|---|
| 底层 | 0.015 × 1.0 = 0.0150 |
| 中层 | 0.015 × 0.7 = 0.0105 |
| 顶层 | 0.015 × 0.5 = 0.0075 |

> 这些分层系数是示例值（贴近"底层靠散热、导热路径短"的物理直觉），可在 `k_map.py` 的 `Z_BREAKS` / `Z_FACTORS` 修改。

---

## 四、k 场形状

```
build_k_field(xc, yc, zc) -> k_field  形状 [1, nx, ny, nz, 1]
```

- `xc/yc/zc` 是坐标列向量 `[nx,1] / [ny,1] / [nz,1]`
- 第 0 维和最后 1 维是广播维，可直接与模型输出 `[batch, nx, ny, nz, 1]` 逐元素相乘
- surface：`[1, 21, 21, 11, 1]`；volume：`[1, 101, 101, 56, 1]`

---

## 五、PDE 残差改造

### 原版（baseline）
```
pde_res = mean((uxx + uyy + uzz)²)     # ∇²T，均匀 k
```

### 3.5D（3d5）
```
pde_res = mean((k_field * (uxx + uyy + uzz))²)   # k·∇²T，分块常数 k
```

这是**简化版**分区：每个区域/层内部 k 不变，所以 `∇·(k∇T) = k·∇²T`。交界线处严格版多出的项，留给阶段 3（界面热流连续）处理。

### volume 版特殊说明

volume 原版 pde_res 已经**按 z 分层**（底层/中间功率层/顶层用不同固定 k，并注入功率项）。3d5 模式在此基础上，把各 z 段的固定 k 替换为 k_field 对应段的值，保留原版的功率注入结构。

---

## 六、运行方式

```bash
# baseline（原版，均匀 k）
python heat_surface.py --model_name DeepOHeat_v1 --mode baseline

# 3.5D（分区 k PDE）
python heat_surface.py --model_name DeepOHeat_v1 --mode 3d5

# volume 版同理
python heat_volumetric.py --model_name DeepOHeat_v1 --mode baseline
python heat_volumetric.py --model_name DeepOHeat_v1 --mode 3d5   # 需先有 volume 三通道数据
```

结果目录自动带 `_mode_baseline` / `_mode_3d5` 后缀。

---

## 七、验证结果（本机 CPU，surface）

| 模式 | 参数量 | loss 趋势 | 说明 |
|---|---|---|---|
| baseline | 805,120 | 807 → 10（8 epochs） | 原版行为不变 ✅ |
| 3d5 分区 k | 1,030,912 | 852 → 25（8 epochs） | 分区 k PDE 正常训练 ✅ |

- k 场四象限取值、z 分层取值均验证正确。
- 两种模式均可训练、loss 正常下降。

---

## 八、验收标准对照

| 验收项 | 状态 |
|---|---|
| 两种模式均可训练 | ✅ baseline / 3d5 都跑通 |
| k 场能正确加载 | ✅ 四象限 + z 分层验证正确 |
| loss 正常下降 | ✅ 两种模式都下降 |
| 不影响原版 V1 运行 | ✅ baseline 参数量 = 原版 805,120，行为一致 |

---

## 九、下一步（阶段 3：界面热流连续）

当前分区 k 已进入 PDE 残差，但**界面交界处**还没做热流连续约束。下一阶段在 `PDE_loss` 加 `bc_interface` 项：界面两侧 `k1·∂u/∂n1 = k2·∂u/∂n2`。mask / interface 输入通道将与损失函数完全联动。
