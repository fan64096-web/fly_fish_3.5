# 第二期数据规格书（P2 数据集 · 草案 v1）

> 目标：在老师《初步工作安排》框架内（80~120 组、常规+极限、测试集预留极端场景），
> 构造一个能区分"物理正确模型"与"简单假设模型"的第二期数据集。
> 动机（对标 v2 论文）：一期数据功率 ~1W、布局单一，均匀 k 假设代价太小；
> 本期把功率提到真实 3.5D 芯片量级并引入布局变化，让分区 k 与结构通道
> 真正发挥作用。所有划分规则在**跑任何仿真之前**预注册（红线）。

---

## 一、维度设计（3 个变化轴）

### 1. 功率轴（让均匀 k 的错误变大）

依据一期实测线性律 Tmax ≈ 24.5×总功率(W) + 25（残差 SD 0.11°C）：

| 档位 | 单 Compute 功率(W) | 单 HBM 功率(W) | 总功率(W) | 预期 Tmax(°C) |
|---|---|---|---|---|
| 常规 | 0.30 ~ 0.90 | 0.12 ~ 0.35 | 1.5 ~ 3.0 | 62 ~ 98 |
| 极端 | 单 Compute 1.4 ~ 2.0（其余 ≤0.15）或单 HBM 0.5 ~ 0.7 | 2.0 ~ 4.0 | 74 ~ 123 |

- 上限取 ~123°C：逼近硅结温上限，物理真实（真实 3.5D 芯片 TDP 数十~数百瓦，
  我们 10×10mm 封装 2~4W 对应同等热密度量级）
- 配比：每布局 20 组 = 14 常规 + 6 极端（30% 极端，与一期一致）
- 功率数值由预注册随机种子生成（seed=20260905），生成后冻结入清单

### 2. 布局轴（让结构通道第一次携带信息）

一期布局完全固定 → mask/界面通道是常量，3d5 的结构感知无用武之地。
本期采用 **5 个离散布局模板**（非连续随机，兼顾 Icepak 建模成本与代码改造量）：

| 模板 | Compute x 范围 | HBM 方块边长 | 特征 |
|---|---|---|---|
| L0 | [0.30, 0.70] | 0.29 | 基准布局（= 一期） |
| L1 | [0.20, 0.60] | 0.25 | Compute 左移 + 小 HBM |
| L2 | [0.40, 0.80] | 0.25 | Compute 右移 + 小 HBM |
| L3 | [0.30, 0.70] | 0.35 | 大 HBM（更贴边缘散热） |
| L4 | [0.25, 0.65] | 0.29 | Compute 左移 + HBM 内缩 0.04 |

- 层叠结构不变（Substrate/Interposer/TIM/die），仅 die 层横向位置变化
- 全部满足不重叠设计规则；Compute 仍为上下两块（y 间隙 0.02），HBM 仍四角
- **每模板只需建 1 个 Icepak 模型**，用 TCL 批量填功率跑 20 组

### 3. HTC 鲁棒性附件（老师 5–6 周"特殊边界工况"）

| 组数 | HTC (W/m²K) | 用途 |
|---|---|---|
| 10 | 2000 / 5000（冷板量级） | 鲁棒性测试：3d5 的 Robin 系数由 HTC 机制化推导（α=k/(HTC·Lz)）可逐样本适配；baseline 的 BC 是写死常数 → 预期差距拉大 |
| 100 | 500（与一期一致） | 主数据集 |

附件组**不进主训练/测试划分**，单独做鲁棒性表（论文加分项）。

---

## 二、样本总量与划分（预注册）

| 项 | 数值 |
|---|---|
| 主数据集 | 5 布局 × 20 功率 = **100 组**（74 常规 + 30 极端…即每布局 14+6） |
| HTC 附件 | 10 组 |
| 测试集 | 每布局固定 2 组（**1 极端 + 1 常规**）= **10 组**（5 极端 + 5 常规） |
| 训练集 | 90 组 |
| 预注册 | seed=20260905 生成全部功率数值与测试集编号，**先冻结入库再开仿真** |

命名：`P2-L{k}-R{01..14}` / `P2-L{k}-E{01..06}`（R=常规 E=极端），
报告导出格式与一期相同（Full report，固体 8820 节点）。

---

## 三、验收标准（对照老师要求）

1. ✅ 样本量 100（80~120 区间）+ 10 组特殊边界
2. ✅ 常规 + 极限热点并存，测试集含极端（每布局 1 组）
3. ✅ 训练/验证/测试划分规则先于仿真冻结，测试集含每布局极端场景
4. ✅ 数据说明文档（meta json 扩展：布局模板 ID、HTC、功率、划分）
5. **科学验收**：一期已证明分区 k 物理更正确（FD 残差 41 倍）；本期验收
   "功率/布局上去后，物理优势是否兑现为精度优势"——预期 baseline 的
   均匀 k 假设误差随功率放大，3d5 结构通道开始携带信息

---

## 四、代码改造清单（与仿真并行）

| 模块 | 改动 | 工作量 |
|---|---|---|
| make_icepak_dataset.py | 解析布局/HTC 列，逐样本生成 mask/interface（不再是常量） | 半天 |
| k_map.py | 5 套模板 k 场（静态），按样本布局 ID 索引 → k 场变 `[B,...]` | 1 天 |
| heat_volumetric.py | PDE/FVM/能量形式接受 batch k 场；BC 系数逐样本化（HTC 维度） | 1~2 天 |
| fd_verify_clean.py | 新训练集上重新标定功率系数（流程不变，红线合规） | 半天 |
| 回归验证 | 布局维度退化为"单一 L0"时应完全复现一期结果 | 半天 |

## 五、TCL 批量仿真（B 岗流程）

1. Icepak 建 5 个布局模型（手工，一次性）
2. TCL 脚本：读功率清单 → 逐组设置 block 功率 → batch solve → 导出 Full report
3. 每布局 20 组自动循环；HTC 附件组改 boundary 条件重跑 10 组
4. 产出 ~110 个 report + 清单文件 → make_icepak_dataset 转换

---

## 六、风险与预案

| 风险 | 预案 |
|---|---|
| 4W 档 Tmax 超预期（非线性） | 先试跑 1 组 4W 极端验证温度，超 125°C 则封顶 3.5W |
| 布局改动导致网格/收敛问题 | 5 模板先各跑 1 组冒烟 |
| 新旧数据分布差异大 | 二期独立成数据集独立训练测试，不做跨期混训（避免归因混乱） |
| 逐样本 k 场改动引入 bug | 回归验证：L0 单布局退化情形必须逐位复现一期 |
